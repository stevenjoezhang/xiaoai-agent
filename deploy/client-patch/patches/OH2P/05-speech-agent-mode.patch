--- squashfs-root/etc/init.d/pns	2026-04-17 16:05:55
+++ squashfs-root-patched/etc/init.d/pns	2026-08-25 12:00:00
@@ -93,5 +93,5 @@
         if [ $VOIP_STATUS_IDLE != $voip_status -o "$VOIP_FLAG" == "voip" ]; then
-            procd_set_param command /usr/bin/mipns-xiaomi -c /usr/share/mipns/ -r opus32 -l -v
+            procd_set_param command /usr/bin/mipns-xiaomi -c /usr/share/mipns/ -l -v
         else
-            procd_set_param command /usr/bin/mipns-xiaomi -c /usr/share/mipns/ -r opus32 -l
+            procd_set_param command /usr/bin/mipns-xiaomi -c /usr/share/mipns/ -l
         fi
--- squashfs-root/etc/rc.local	2026-08-25 11:59:00
+++ squashfs-root-patched/etc/rc.local	2026-08-25 12:00:00
@@ -4 +4,23 @@
-[ -f "/data/init.sh" ] && sh /data/init.sh >/dev/null 2>&1 &
+[ -f "/data/init.sh" ] && (
+    /etc/init.d/pns stop
+    if [ -e "/data/mipns/dialog_continuous" ]; then
+        mv -f "/data/mipns/dialog_continuous" \
+            "/data/mipns/dialog_continuous.xiaoai-agent-disabled"
+    fi
+    i=0
+    while [ "$i" -lt 30 ] && \
+          { [ ! -S "/tmp/mico_aivs_lab/usock/common.usock" ] || \
+            [ ! -S "/tmp/mico_aivs_lab/usock/speech.usock" ]; }; do
+        sleep 1
+        i=$((i + 1))
+    done
+    sh /data/init.sh >/dev/null 2>&1
+    i=0
+    while [ "$i" -lt 30 ] && [ ! -S "/tmp/mico_aivs_lab/usock/speech.usock" ]; do
+        sleep 1
+        i=$((i + 1))
+    done
+    if [ -S "/tmp/mico_aivs_lab/usock/speech.usock" ]; then
+        /etc/init.d/pns restart
+    fi
+) &
